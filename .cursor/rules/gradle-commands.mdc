---
description: Gradle commands have moved to shell-commands.mdc
globs: "*.gradle.kts"
alwaysApply: false
---

# Gradle Commands

The `./gradlew` command is now documented in `.cursor/rules/shell-commands.mdc`.

This file is kept for reference but `shell-commands.mdc` is the authoritative source for running gradle commands.
